FROM rust:alpine
# mingw-w64-gcc cross-compiles the bundled SQLite C source for Windows.
RUN apk add --no-cache musl-dev gcc mingw-w64-gcc \
    && rustup target add x86_64-pc-windows-gnu
WORKDIR /src
